O hoisting é quando o Javascript verifica a presença de funções antes da execução do código.
Por essa razão, uma função poderá ser chamada em qualquer local do código, independente de onde a função em si está localizada no código.

Ex:

foo();
function foo(){
    console.log("bar");
}